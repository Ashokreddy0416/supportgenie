[project]
name = "supportgenie"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "datasets>=5.0.0",
    "pandas>=3.0.3",
    "sentence-transformers>=5.6.0",
]
